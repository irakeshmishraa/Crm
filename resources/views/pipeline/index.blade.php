@extends('layouts.app')
@section('title', 'Sales Pipeline')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h4 class="fw-bold mb-0">Sales Pipeline</h4><a href="{{ route('admin.pipeline.forecast') }}" class="btn btn-outline-primary btn-sm"><i class="bi bi-graph-up me-1"></i>Forecast</a></div>
<div class="d-flex gap-2 overflow-auto pb-2 mb-3">@foreach($stageStats as $key=>$stat)<div class="badge bg-light text-dark border px-3 py-2"><div class="small fw-normal">{{ $stat['label'] }}</div><div class="fw-bold">{{ $stat['count'] }} leads</div></div>@endforeach</div>
<div class="d-flex gap-3 overflow-auto pb-3" style="min-height:600px">@foreach($stages as $stageKey=>$stageLabel)<div class="kanban-column bg-light rounded-3 p-3 flex-shrink-0" style="min-width:300px"><div class="d-flex justify-content-between align-items-center mb-3"><h6 class="fw-bold mb-0">{{ $stageLabel }}</h6><span class="badge bg-primary">{{ ($leads->get($stageKey) ?? collect())->count() }}</span></div><div class="kanban-body d-flex flex-column gap-2" data-stage="{{ $stageKey }}">@foreach(($leads->get($stageKey) ?? collect()) as $lead)<div class="kanban-card card border-0 shadow-sm" data-lead-id="{{ $lead->id }}"><div class="card-body p-3"><div class="d-flex justify-content-between align-items-start mb-2"><a href="{{ route('admin.leads.show', $lead) }}" class="fw-semibold text-decoration-none">{{ $lead->name }}</a><span class="badge bg-{{ $lead->priority_badge }}">{{ ucfirst($lead->priority) }}</span></div><small class="text-muted">{{ $lead->company_name }}</small>@if($lead->deal_value)<div class="mt-2 fw-bold text-success">{{ format_currency($lead->deal_value) }}</div>@endif</div></div>@endforeach</div></div>@endforeach</div>
@endsection
@push('scripts')<script>document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('.kanban-body').forEach(col=>{new Sortable(col,{group:'pipeline',animation:150,onEnd:function(e){const leadId=e.item.dataset.leadId;const stage=e.to.dataset.stage;fetch('/admin/pipeline/update-stage',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-TOKEN':document.querySelector('meta[name=csrf-token]').content},body:JSON.stringify({lead_id:leadId,stage:stage})})}})})});</script>@endpush
