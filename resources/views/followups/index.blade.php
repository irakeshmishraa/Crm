@extends('layouts.app')
@section('title', 'Follow-Ups')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h4 class="fw-bold mb-0">Follow-Ups</h4><div class="d-flex gap-2"><a href="{{ route('admin.followups.calendar') }}" class="btn btn-outline-secondary btn-sm"><i class="bi bi-calendar3 me-1"></i>Calendar</a><a href="{{ route('admin.followups.create') }}" class="btn btn-primary btn-sm"><i class="bi bi-plus-lg me-1"></i>New Follow-Up</a></div></div>
<div class="card border-0 shadow-sm mb-3"><div class="card-body py-3"><form method="GET" class="row g-2"><div class="col-md-3"><select name="status" class="form-select form-select-sm"><option value="">All Status</option>@foreach(config('crm.followup_statuses') as $k=>$v)<option value="{{ $k }}" {{ request('status')==$k?'selected':'' }}>{{ $v }}</option>@endforeach</select></div><div class="col-md-3"><select name="type" class="form-select form-select-sm"><option value="">All Types</option>@foreach(config('crm.followup_types') as $k=>$v)<option value="{{ $k }}" {{ request('type')==$k?'selected':'' }}>{{ $v }}</option>@endforeach</select></div><div class="col-md-3"><input type="date" name="date" class="form-control form-control-sm" value="{{ request('date') }}"></div><div class="col-md-2"><button type="submit" class="btn btn-sm btn-primary w-100"><i class="bi bi-funnel"></i> Filter</button></div></form></div></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead class="bg-light"><tr><th>Type</th><th>Title</th><th>Lead</th><th>Scheduled</th><th>Status</th><th>Assigned</th><th>Actions</th></tr></thead><tbody>@forelse($followups as $f)<tr class="{{ $f->isOverdue()?'table-danger':'' }}"><td><span class="badge bg-{{ $f->type_badge }}">{{ ucfirst($f->type) }}</span></td><td><a href="{{ route('admin.followups.show', $f) }}" class="text-decoration-none fw-semibold">{{ $f->title }}</a></td><td>{{ $f->lead?->name ?? '-' }}</td><td>{{ $f->scheduled_at->format('d M Y, h:i A') }}@if($f->isOverdue())<br><small class="text-danger fw-bold">OVERDUE</small>@endif</td><td><span class="badge bg-{{ $f->status_badge }}">{{ ucfirst($f->status) }}</span></td><td>{{ $f->assignedTo?->name }}</td><td><div class="btn-group btn-group-sm">@if($f->status==='pending')<form action="{{ route('admin.followups.complete', $f) }}" method="POST" class="d-inline">@csrf<button class="btn btn-success btn-sm" title="Complete"><i class="bi bi-check-lg"></i></button></form>@endif<a href="{{ route('admin.followups.edit', $f) }}" class="btn btn-outline-secondary btn-sm"><i class="bi bi-pencil"></i></a></div></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">No follow-ups found.</td></tr>@endforelse</tbody></table></div>@if($followups->hasPages())<div class="card-footer bg-transparent">{{ $followups->links() }}</div>@endif</div>
@endsection
